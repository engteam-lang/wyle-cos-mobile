plugins {
    id "com.android.application"
    id "dev.flutter.flutter-gradle-plugin"
}

// Apply Kotlin via legacy DSL so it reads from the buildscript classpath
// (1.9.24 declared in the top-level build.gradle) rather than going through
// the plugins-DSL resolver where Flutter's composite-build can intercept it.
apply plugin: 'kotlin-android'

// Firebase / FCM — must come AFTER com.android.application
apply plugin: 'com.google.gms.google-services'

// Flutter's Gradle plugin normally redirects build output to
// {project_root}/build/app/ so `flutter build apk` can locate the APK.
// In some CI environments this redirect doesn't take effect, and Gradle
// falls back to android/app/build/. Set it explicitly here to guarantee
// the APK always lands where Flutter expects it.
layout.buildDirectory = rootProject.layout.projectDirectory.dir("../build/app")

android {
    namespace "com.wyle.wylecosapp"
    // compileSdk must be 36: flutter_secure_storage and flutter_tts both set
    // compileSdk 36 in their own Gradle files and the Android Gradle Plugin
    // requires compileSdk >= max(plugin compileSdk values).
    // targetSdk stays at flutter.targetSdkVersion (35) — backward compatible.
    compileSdk 36
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    sourceSets {
        main.java.srcDirs += "src/main/kotlin"
    }

    defaultConfig {
        applicationId "com.wyle.wylecosapp"
        minSdk 24
        targetSdk flutter.targetSdkVersion
        versionCode flutter.versionCode
        versionName flutter.versionName
    }

    buildTypes {
        release {
            // Uses debug signing for now — add a proper keystore for production
            signingConfig signingConfigs.debug
            minifyEnabled false
            shrinkResources false
        }
    }
}

flutter {
    source "../.."
}
